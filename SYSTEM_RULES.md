# 🛡️ SYSTEM_RULES (체육관 운영 원칙 및 개발 지침)

이 문서는 라이온개발자 프로젝트의 영구 기억(Persistent Knowledge)으로 작용합니다. AI 어시스턴트는 코드를 작성하거나 디버깅할 때 **반드시 아래의 원칙들을 최우선으로 준수**해야 합니다.

---

## 1. 프로젝트 정체성 및 맥락 (Project Context)
- **주요 도메인**: 체육관(합기도, 태권도 등 무도 도장) 회원 관리 프로그램, 네이버 블로그/밴드/카페 마케팅 자동화, 주식 API(한국투자증권) 연동 등.
- **포스팅 감수성**: 체육관의 일상 수련(체력, 민첩성, 호신술, 낙법 등)과 특수 이벤트(대회, 주말 캠프, 심사)를 명확히 구분하여, 학부모와 일반인이 보기에 자연스럽고 전문적인 포스팅이 작성되도록 유지합니다.

## 2. 언어 및 코딩 표준 (Language & Coding Standards)
- **설명 및 답변**: 모든 설명과 보고는 **'한국어'**로 정중하고 명확하게 작성합니다.
- **코드 명명 규칙**: 코드 내의 변수명, 함수명, 클래스명 및 주석은 글로벌 규격에 맞춰 **'영어'**로 작성하여 범용성을 확보합니다.
- **환경 관리**: 파이썬 가상환경은 `uv`를 사용하며, 폴더명은 `.venv`로 고정합니다. (기존 `.venv` 환경을 무조건 재사용)

## 3. 디버깅 및 무결성 유지 5대 절대 규칙 (Integrity Rules)
1. **새 파일 생성 금지**: 기능 추가나 수정은 가급적 기존 파일 내부에서 처리하여 파일 파편화를 방지합니다.
2. **하위 호환성 유지**: 기존의 클래스명, 함수명, 변수명을 임의로 변경하지 않습니다.
3. **레이아웃 유지**: 프로젝트 전체 폴더 구조 및 기존 Flet UI 레이아웃의 틀을 함부로 바꾸지 않습니다.
4. **리팩토링 최소화**: 오류와 관련 없는 부분의 불필요한 자동 리팩토링이나 코드 이동을 절대 금지합니다.
5. **원인 분석(RCA) 선행**: 에러 발생 시, 단순히 코드를 덮어쓰기 전에 에러의 근본 원인을 먼저 분석하고 설명해야 합니다.

## 4. 스마트 이미지 매칭 알고리즘 원칙 (Smart Matching Rules)
- **엄격한 특수 폴더 필터링**: "대회", "행사,캠프,체험" 등 특수 이벤트성 폴더는 본문(Content)에 일상적인 단어('경기', '선수', '체험')가 우연히 포함되었다고 해서 오매칭되어서는 안 됩니다. **반드시 제목(Title)에 명시적인 핵심 어근(예: 품새대회, 메달, 야외활동 등)이 포함될 때만 매칭**되도록 합니다.
- **안전한 폴백 (Fallback)**: 텍스트(어근/유사어)가 확실히 매칭되지 않으면 억지로 매칭시키지 않고 반드시 `None`을 반환하여, 1번부터 10번까지 번갈아 나오는 기본 순차 순환 폴더(`default_images_N`)가 정상적으로 작동하게 합니다.
- **어근 중심 학습**: AI 학습 시, 폴더명의 핵심 글씨(어근)가 무조건 포함된 확실한 파생 단어 위주로만 규칙을 생성해야 합니다.

## 5. 무한 루프 방지 및 안전 예외 처리 (Safe Exception Handling)
- **에러 반복 방지**: Selenium 등에서 동일한 오류가 2회 이상 반복되면 단순 재시도를 멈추고 접근 방식 자체(최신 셀렉터 제안 등)를 점검합니다.
- **안전한 종료**: 무한 대기(Infinite Wait)를 방지하기 위해 특정 시간 경과 시 에러 로그를 남기고 안전하게 루틴을 빠져나오거나 순환 로직으로 복귀하도록 구현합니다.
- **표준화된 로깅**: 모든 주요 액션(클릭, 페이지 로딩, 매칭 완료 등) 전후에 `[Step N] [모듈명] 동작 설명 (상태)` 형태의 표준화된 로깅을 출력하여 가시성을 높입니다.

---
> **💡 AI 지침**: 위 규칙은 체육관 프로그램의 장기적인 안정성을 위한 관장님의 철학이 담겨 있습니다. 향후 모든 업데이트 시 이 `SYSTEM_RULES.md`를 기준점으로 삼아 판단하십시오.
