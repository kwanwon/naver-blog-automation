# 🦁 라이온개발자 프로젝트 AI 코딩 가이드라인 (절대 수정 금지)

이 파일은 AI(코딩 어시스턴트)가 코드를 수정하거나 새로운 기능을 추가할 때 **과거의 버그 상태로 회귀(Regression)하는 것을 막기 위한 핵심 규칙서**입니다.
AI는 작업을 시작하기 전 반드시 이 문서의 규칙을 준수해야 하며, 사용자의 명시적인 허락 없이 아래 로직을 변경하거나 삭제해서는 안 됩니다.

---

## 🛑 핵심 고정 로직 (Do Not Touch)

### 1. 네이버 블로그 가독성 (줄바꿈) 강제 훼손 금지
- **대상 파일:** `naver_blog_auto.py`
- **대상 함수:** `should_add_blank_line`
- **규칙:** 이 함수는 **반드시 `return False`로 고정**되어야 합니다.
- **이유:** AI가 이미 완벽하게 계산한 문단 줄바꿈(`\n\n`)과 사용자 설정 슬로건의 줄바꿈을 셀레니움 자동화 모듈이 임의로(3줄 이상 연속 시 등) 훼손하는 것을 원천 차단하기 위함입니다.

### 2. 블로그 자동 태그 생성 갯수 강제 (15개)
- **대상 파일:** `modules/ai_experts/blog_expert.py`
- **대상 변수/프롬프트:** `base_prompt` 내의 `[태그]` 섹션 지시어
- **규칙:** 태그 지시어는 반드시 **"글과 유기적으로 긴밀히 매칭되는 태그 정확히 15개"**를 생성하도록 프롬프트에 고정해야 합니다. (5~10개 이하 등 유동적인 숫자 금지)
- **이유:** 사용자 고정 태그 15개와 AI 자동 생성 태그 15개를 합쳐 네이버 블로그 최대 허용치인 30개를 꽉 채우기 위함입니다.

### 3. 이미지 순환 폴더 (빈 폴더 자동 스킵 로직)
- **대상 파일:** `blog_writer_app.py`
- **대상 함수:** `get_next_image_folder`
- **규칙:** `default_images_1` ~ `default_images_10`을 순환할 때 단순히 폴더의 존재 여부(`os.path.exists`)만 확인해서는 안 됩니다. 반드시 `has_images` 헬퍼 함수를 통해 **폴더 내부에 실제 이미지/동영상 파일(jpg, png, mp4 등)이 존재하는지 검사**해야 합니다. 빈 폴더(사진 0장)일 경우 멈추지 않고 즉시 다음 번호 폴더로 넘어가야 합니다.
- **안전장치:** 무한 반복을 막기 위해 `for _ in range(10):` 처럼 딱 10번만 검사하도록 제한해야 합니다. 이전 포스팅 때 썼던 폴더 인덱스는 반드시 기억하여 다음 번호부터 이어서 순환해야 합니다.

### 4. 플랫폼 간 격리 원칙 (Platform Isolation)
- **대상 파일:** `modules/ai_experts/base_expert.py` 및 플랫폼별 expert 파일
- **규칙:** 네이버 블로그 전용 로직을 수정할 때, **공통 모듈인 `base_expert.py`를 무분별하게 수정하여 네이버 밴드나 네이버 카페 자동화에 사이드 이펙트(부작용)를 발생시키면 절대 안 됩니다.** 플랫폼마다 고유의 동작이 보장되도록 철저히 분리해서 작업하세요.

---
**🤖 AI에게 보내는 메시지:** 
당신이 이 프로젝트의 코드를 수정 중이라면, 위 사항들이 현재 코드에 잘 유지되고 있는지 항상 교차 검증하십시오.
